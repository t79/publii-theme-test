	
	{{#if @config.custom.backgroundPicture}}
			<div id="background-image-container">
				<img src="{{@config.custom.backgroundPicture}}" id="background-image" 
					{{lazyload "lazy"}} alt="">
			</div>
	{{/if}}

	</body>
	
</html>